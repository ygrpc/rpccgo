// Code generated by protoc-gen-rpc-cgo-dart. DO NOT EDIT.
//
// Source: greeter.proto

import 'dart:convert' as convert;
import 'dart:ffi' as ffi;
import 'dart:typed_data' as typed_data;

import 'package:ffi/ffi.dart' as pkg_ffi;

import 'greeter.pb.dart' as pb;

// rpccgo Dart FFI message client generated file for Greeter

typedef _RpccgoReleaseNative = ffi.Int32 Function(ffi.UintPtr ptr);
typedef _RpccgoRelease = int Function(int ptr);
typedef _RpccgoTakeErrorTextNative =
    ffi.Int32 Function(
      ffi.Int32 errID,
      ffi.Pointer<ffi.UintPtr> textPtr,
      ffi.Pointer<ffi.Int32> textLen,
    );
typedef _RpccgoTakeErrorText =
    int Function(
      int errID,
      ffi.Pointer<ffi.UintPtr> textPtr,
      ffi.Pointer<ffi.Int32> textLen,
    );
typedef _RpccgoMessageUnaryNative =
    ffi.Int32 Function(
      ffi.UintPtr requestPtr,
      ffi.Int32 requestLen,
      ffi.Pointer<ffi.UintPtr> responsePtr,
      ffi.Pointer<ffi.Int32> responseLen,
    );
typedef _RpccgoMessageUnary =
    int Function(
      int requestPtr,
      int requestLen,
      ffi.Pointer<ffi.UintPtr> responsePtr,
      ffi.Pointer<ffi.Int32> responseLen,
    );
typedef _RpccgoStreamStartNative =
    ffi.Int32 Function(ffi.Pointer<ffi.Int32> handle);
typedef _RpccgoStreamStart = int Function(ffi.Pointer<ffi.Int32> handle);
typedef _RpccgoServerStreamStartNative =
    ffi.Int32 Function(
      ffi.UintPtr requestPtr,
      ffi.Int32 requestLen,
      ffi.Pointer<ffi.Int32> handle,
    );
typedef _RpccgoServerStreamStart =
    int Function(int requestPtr, int requestLen, ffi.Pointer<ffi.Int32> handle);
typedef _RpccgoStreamSendNative =
    ffi.Int32 Function(
      ffi.Int32 handle,
      ffi.UintPtr requestPtr,
      ffi.Int32 requestLen,
    );
typedef _RpccgoStreamSend =
    int Function(int handle, int requestPtr, int requestLen);
typedef _RpccgoStreamReadNative =
    ffi.Int32 Function(
      ffi.Int32 handle,
      ffi.Pointer<ffi.UintPtr> responsePtr,
      ffi.Pointer<ffi.Int32> responseLen,
    );
typedef _RpccgoStreamRead =
    int Function(
      int handle,
      ffi.Pointer<ffi.UintPtr> responsePtr,
      ffi.Pointer<ffi.Int32> responseLen,
    );
typedef _RpccgoStreamFinishNative =
    ffi.Int32 Function(
      ffi.Int32 handle,
      ffi.Pointer<ffi.UintPtr> responsePtr,
      ffi.Pointer<ffi.Int32> responseLen,
    );
typedef _RpccgoStreamFinish =
    int Function(
      int handle,
      ffi.Pointer<ffi.UintPtr> responsePtr,
      ffi.Pointer<ffi.Int32> responseLen,
    );
typedef _RpccgoStreamFinishVoidNative = ffi.Int32 Function(ffi.Int32 handle);
typedef _RpccgoStreamFinishVoid = int Function(int handle);
typedef _RpccgoStreamCancelNative = ffi.Int32 Function(ffi.Int32 handle);
typedef _RpccgoStreamCancel = int Function(int handle);

class GreeterRpccgoClient {
  GreeterRpccgoClient(ffi.DynamicLibrary library)
    : _release = library.lookupFunction<_RpccgoReleaseNative, _RpccgoRelease>(
        'rpccgo_release',
      ),
      _takeErrorText = library
          .lookupFunction<_RpccgoTakeErrorTextNative, _RpccgoTakeErrorText>(
            'rpccgo_take_error_text',
          ),
      _sayHello = library
          .lookupFunction<_RpccgoMessageUnaryNative, _RpccgoMessageUnary>(
            'rpccgo_msg_greeterv1_Greeter_SayHello',
          ),
      _collectStart = library
          .lookupFunction<_RpccgoStreamStartNative, _RpccgoStreamStart>(
            'rpccgo_msg_greeterv1_Greeter_Collect_start',
          ),
      _collectSend = library
          .lookupFunction<_RpccgoStreamSendNative, _RpccgoStreamSend>(
            'rpccgo_msg_greeterv1_Greeter_Collect_send',
          ),
      _collectFinish = library
          .lookupFunction<_RpccgoStreamFinishNative, _RpccgoStreamFinish>(
            'rpccgo_msg_greeterv1_Greeter_Collect_finish',
          ),
      _collectCancel = library
          .lookupFunction<_RpccgoStreamCancelNative, _RpccgoStreamCancel>(
            'rpccgo_msg_greeterv1_Greeter_Collect_cancel',
          ),
      _broadcastStart = library
          .lookupFunction<
            _RpccgoServerStreamStartNative,
            _RpccgoServerStreamStart
          >('rpccgo_msg_greeterv1_Greeter_Broadcast_start'),
      _broadcastRead = library
          .lookupFunction<_RpccgoStreamReadNative, _RpccgoStreamRead>(
            'rpccgo_msg_greeterv1_Greeter_Broadcast_read',
          ),
      _broadcastFinish = library
          .lookupFunction<
            _RpccgoStreamFinishVoidNative,
            _RpccgoStreamFinishVoid
          >('rpccgo_msg_greeterv1_Greeter_Broadcast_finish'),
      _broadcastCancel = library
          .lookupFunction<_RpccgoStreamCancelNative, _RpccgoStreamCancel>(
            'rpccgo_msg_greeterv1_Greeter_Broadcast_cancel',
          ),
      _chatStart = library
          .lookupFunction<_RpccgoStreamStartNative, _RpccgoStreamStart>(
            'rpccgo_msg_greeterv1_Greeter_Chat_start',
          ),
      _chatSend = library
          .lookupFunction<_RpccgoStreamSendNative, _RpccgoStreamSend>(
            'rpccgo_msg_greeterv1_Greeter_Chat_send',
          ),
      _chatRead = library
          .lookupFunction<_RpccgoStreamReadNative, _RpccgoStreamRead>(
            'rpccgo_msg_greeterv1_Greeter_Chat_read',
          ),
      _chatCloseSend = library
          .lookupFunction<
            _RpccgoStreamFinishVoidNative,
            _RpccgoStreamFinishVoid
          >('rpccgo_msg_greeterv1_Greeter_Chat_close_send'),
      _chatFinish = library
          .lookupFunction<
            _RpccgoStreamFinishVoidNative,
            _RpccgoStreamFinishVoid
          >('rpccgo_msg_greeterv1_Greeter_Chat_finish'),
      _chatCancel = library
          .lookupFunction<_RpccgoStreamCancelNative, _RpccgoStreamCancel>(
            'rpccgo_msg_greeterv1_Greeter_Chat_cancel',
          );

  final _RpccgoRelease _release;
  final _RpccgoTakeErrorText _takeErrorText;
  final _RpccgoMessageUnary _sayHello;
  final _RpccgoStreamStart _collectStart;
  final _RpccgoStreamSend _collectSend;
  final _RpccgoStreamFinish _collectFinish;
  final _RpccgoStreamCancel _collectCancel;
  final _RpccgoServerStreamStart _broadcastStart;
  final _RpccgoStreamRead _broadcastRead;
  final _RpccgoStreamFinishVoid _broadcastFinish;
  final _RpccgoStreamCancel _broadcastCancel;
  final _RpccgoStreamStart _chatStart;
  final _RpccgoStreamSend _chatSend;
  final _RpccgoStreamRead _chatRead;
  final _RpccgoStreamFinishVoid _chatCloseSend;
  final _RpccgoStreamFinishVoid _chatFinish;
  final _RpccgoStreamCancel _chatCancel;

  pb.SayHelloResponse SayHello(pb.SayHelloRequest request) {
    final responsePtr = pkg_ffi.calloc<ffi.UintPtr>();
    final responseLen = pkg_ffi.calloc<ffi.Int32>();
    final requestBytes = request.writeToBuffer();
    final requestPtr = _allocateBytes(requestBytes);
    try {
      final errID = _sayHello(
        requestPtr.address,
        requestBytes.length,
        responsePtr,
        responseLen,
      );
      _throwIfError(errID);
      final responseBytes = _takeBytes(responsePtr.value, responseLen.value);
      return pb.SayHelloResponse.fromBuffer(responseBytes);
    } finally {
      pkg_ffi.calloc.free(requestPtr);
      pkg_ffi.calloc.free(responsePtr);
      pkg_ffi.calloc.free(responseLen);
    }
  }

  GreeterCollectStream Collect() {
    final handlePtr = pkg_ffi.calloc<ffi.Int32>();
    try {
      final errID = _collectStart(handlePtr);
      _throwIfError(errID);
      return GreeterCollectStream._(this, handlePtr.value);
    } finally {
      pkg_ffi.calloc.free(handlePtr);
    }
  }

  GreeterBroadcastStream Broadcast(pb.SayHelloRequest request) {
    final handlePtr = pkg_ffi.calloc<ffi.Int32>();
    final requestBytes = request.writeToBuffer();
    final requestPtr = _allocateBytes(requestBytes);
    try {
      final errID = _broadcastStart(
        requestPtr.address,
        requestBytes.length,
        handlePtr,
      );
      _throwIfError(errID);
      return GreeterBroadcastStream._(this, handlePtr.value);
    } finally {
      pkg_ffi.calloc.free(requestPtr);
      pkg_ffi.calloc.free(handlePtr);
    }
  }

  GreeterChatStream Chat() {
    final handlePtr = pkg_ffi.calloc<ffi.Int32>();
    try {
      final errID = _chatStart(handlePtr);
      _throwIfError(errID);
      return GreeterChatStream._(this, handlePtr.value);
    } finally {
      pkg_ffi.calloc.free(handlePtr);
    }
  }

  ffi.Pointer<ffi.Uint8> _allocateBytes(List<int> data) {
    final ptr = pkg_ffi.calloc<ffi.Uint8>(data.length);
    ptr.asTypedList(data.length).setAll(0, data);
    return ptr;
  }

  typed_data.Uint8List _takeBytes(int ptr, int length) {
    if (length < 0) {
      throw StateError('rpccgo: response length is negative');
    }
    if (length == 0) {
      return typed_data.Uint8List(0);
    }
    if (ptr == 0) {
      throw StateError('rpccgo: response pointer is nil');
    }
    final data = typed_data.Uint8List.fromList(
      ffi.Pointer<ffi.Uint8>.fromAddress(ptr).asTypedList(length),
    );
    final status = _release(ptr);
    if (status != 0) {
      throw StateError('rpccgo: failed to release response buffer');
    }
    return data;
  }

  void _throwIfError(int errID) {
    if (errID == 0) {
      return;
    }
    throw StateError(_takeError(errID));
  }

  String _takeError(int errID) {
    final textPtr = pkg_ffi.calloc<ffi.UintPtr>();
    final textLen = pkg_ffi.calloc<ffi.Int32>();
    try {
      final status = _takeErrorText(errID, textPtr, textLen);
      if (status != 0) {
        return 'rpccgo error $errID';
      }
      final data = _takeBytes(textPtr.value, textLen.value);
      return convert.utf8.decode(data);
    } finally {
      pkg_ffi.calloc.free(textPtr);
      pkg_ffi.calloc.free(textLen);
    }
  }
}

class GreeterCollectStream {
  GreeterCollectStream._(this._client, this._handle);
  final GreeterRpccgoClient _client;
  final int _handle;
  void send(pb.SayHelloRequest request) {
    final requestBytes = request.writeToBuffer();
    final requestPtr = _client._allocateBytes(requestBytes);
    try {
      final errID = _client._collectSend(
        _handle,
        requestPtr.address,
        requestBytes.length,
      );
      _client._throwIfError(errID);
    } finally {
      pkg_ffi.calloc.free(requestPtr);
    }
  }

  pb.SayHelloResponse finish() {
    final responsePtr = pkg_ffi.calloc<ffi.UintPtr>();
    final responseLen = pkg_ffi.calloc<ffi.Int32>();
    try {
      final errID = _client._collectFinish(_handle, responsePtr, responseLen);
      _client._throwIfError(errID);
      final responseBytes = _client._takeBytes(
        responsePtr.value,
        responseLen.value,
      );
      return pb.SayHelloResponse.fromBuffer(responseBytes);
    } finally {
      pkg_ffi.calloc.free(responsePtr);
      pkg_ffi.calloc.free(responseLen);
    }
  }

  void cancel() {
    final errID = _client._collectCancel(_handle);
    _client._throwIfError(errID);
  }
}

class GreeterBroadcastStream {
  GreeterBroadcastStream._(this._client, this._handle);
  final GreeterRpccgoClient _client;
  final int _handle;
  pb.SayHelloResponse read() {
    final responsePtr = pkg_ffi.calloc<ffi.UintPtr>();
    final responseLen = pkg_ffi.calloc<ffi.Int32>();
    try {
      final errID = _client._broadcastRead(_handle, responsePtr, responseLen);
      _client._throwIfError(errID);
      final responseBytes = _client._takeBytes(
        responsePtr.value,
        responseLen.value,
      );
      return pb.SayHelloResponse.fromBuffer(responseBytes);
    } finally {
      pkg_ffi.calloc.free(responsePtr);
      pkg_ffi.calloc.free(responseLen);
    }
  }

  void finish() {
    final errID = _client._broadcastFinish(_handle);
    _client._throwIfError(errID);
  }

  void cancel() {
    final errID = _client._broadcastCancel(_handle);
    _client._throwIfError(errID);
  }
}

class GreeterChatStream {
  GreeterChatStream._(this._client, this._handle);
  final GreeterRpccgoClient _client;
  final int _handle;
  void send(pb.SayHelloRequest request) {
    final requestBytes = request.writeToBuffer();
    final requestPtr = _client._allocateBytes(requestBytes);
    try {
      final errID = _client._chatSend(
        _handle,
        requestPtr.address,
        requestBytes.length,
      );
      _client._throwIfError(errID);
    } finally {
      pkg_ffi.calloc.free(requestPtr);
    }
  }

  pb.SayHelloResponse read() {
    final responsePtr = pkg_ffi.calloc<ffi.UintPtr>();
    final responseLen = pkg_ffi.calloc<ffi.Int32>();
    try {
      final errID = _client._chatRead(_handle, responsePtr, responseLen);
      _client._throwIfError(errID);
      final responseBytes = _client._takeBytes(
        responsePtr.value,
        responseLen.value,
      );
      return pb.SayHelloResponse.fromBuffer(responseBytes);
    } finally {
      pkg_ffi.calloc.free(responsePtr);
      pkg_ffi.calloc.free(responseLen);
    }
  }

  void closeSend() {
    final errID = _client._chatCloseSend(_handle);
    _client._throwIfError(errID);
  }

  void finish() {
    final errID = _client._chatFinish(_handle);
    _client._throwIfError(errID);
  }

  void cancel() {
    final errID = _client._chatCancel(_handle);
    _client._throwIfError(errID);
  }
}
