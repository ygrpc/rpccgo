// Code generated by protoc-gen-rpc-cgo-jni. DO NOT EDIT.
//
// Source: foreground_service.proto

#include <jni.h>
#include <stdint.h>
#include <stdlib.h>

#include <mutex>
#include <string>
#include <vector>

#include "librpccgo_android_foreground_service.h"

JavaVM* javaVM = nullptr;

JNIEXPORT jint JNICALL JNI_OnLoad(JavaVM* vm, void*) {
    javaVM = vm;
    // Android supports JNI 1.6; the JavaVM is also used to resolve JNIEnv on stream operation threads.
    return JNI_VERSION_1_6;
}

JNIEXPORT void JNICALL JNI_OnUnload(JavaVM*, void*) {
    javaVM = nullptr;
}

namespace {

class rpccgoJNIEnvScope {
public:
    explicit rpccgoJNIEnvScope(JNIEnv* current) : env(current), attached(false) {
        if (env != nullptr || javaVM == nullptr) { return; }
        void* rawEnv = nullptr;
        jint status = javaVM->GetEnv(&rawEnv, JNI_VERSION_1_6);
        if (status == JNI_OK) {
            env = static_cast<JNIEnv*>(rawEnv);
            return;
        }
        JNIEnv* attachedEnv = nullptr;
        if (status == JNI_EDETACHED && javaVM->AttachCurrentThread(&attachedEnv, nullptr) == JNI_OK) {
            env = attachedEnv;
            attached = true;
        }
    }
    ~rpccgoJNIEnvScope() {
        if (attached && javaVM != nullptr) { javaVM->DetachCurrentThread(); }
    }
    JNIEnv* env;

private:
    bool attached;
};

jbyteArray rpccgoJNIByteArray(JNIEnv* env, const std::vector<uint8_t>& data) {
    if (env == nullptr) { return nullptr; }
    jbyteArray array = env->NewByteArray(static_cast<jsize>(data.size()));
    if (array == nullptr) { return nullptr; }
    if (!data.empty()) {
        env->SetByteArrayRegion(array, 0, static_cast<jsize>(data.size()), reinterpret_cast<const jbyte*>(data.data()));
    }
    return array;
}

std::vector<uint8_t> rpccgoJNIBytes(JNIEnv* env, jbyteArray value, bool* ok) {
    if (ok != nullptr) { *ok = false; }
    if (env == nullptr || value == nullptr) { return {}; }
    jsize length = env->GetArrayLength(value);
    if (length < 0) { return {}; }
    std::vector<uint8_t> data(static_cast<size_t>(length));
    if (length != 0) {
        env->GetByteArrayRegion(value, 0, length, reinterpret_cast<jbyte*>(data.data()));
    }
    if (ok != nullptr) { *ok = !env->ExceptionCheck(); }
    return data;
}

void rpccgoWriteInt32(std::vector<uint8_t>* out, int32_t value) {
    out->push_back(static_cast<uint8_t>((value >> 24) & 0xff));
    out->push_back(static_cast<uint8_t>((value >> 16) & 0xff));
    out->push_back(static_cast<uint8_t>((value >> 8) & 0xff));
    out->push_back(static_cast<uint8_t>(value & 0xff));
}

jbyteArray rpccgoResult(JNIEnv* env, bool ok, const std::vector<uint8_t>& payload) {
    std::vector<uint8_t> out;
    out.reserve(payload.size() + 5);
    out.push_back(ok ? 1 : 0);
    rpccgoWriteInt32(&out, static_cast<int32_t>(payload.size()));
    out.insert(out.end(), payload.begin(), payload.end());
    return rpccgoJNIByteArray(env, out);
}

std::vector<uint8_t> rpccgoErrorText(int32_t errID) {
    uintptr_t textPtr = 0;
    int32_t textLen = 0;
    int32_t status = rpccgoTakeErrorText(errID, &textPtr, &textLen);
    if (status != 0 || textLen < 0 || textPtr == 0) {
        std::string fallback = "rpccgo: unknown error id " + std::to_string(errID);
        return std::vector<uint8_t>(fallback.begin(), fallback.end());
    }
    const uint8_t* data = reinterpret_cast<const uint8_t*>(textPtr);
    std::vector<uint8_t> text(data, data + textLen);
    rpccgoRelease(textPtr);
    return text;
}

jbyteArray rpccgoErrorResult(JNIEnv* env, const std::string& message) {
    return rpccgoResult(env, false, std::vector<uint8_t>(message.begin(), message.end()));
}

jbyteArray rpccgoErrorIDResult(JNIEnv* env, int32_t errID) {
    return rpccgoResult(env, false, rpccgoErrorText(errID));
}

jbyteArray rpccgoSuccessBytes(JNIEnv* env, uintptr_t responsePtr, int32_t responseLen) {
    if (responseLen < 0) { return rpccgoErrorResult(env, "rpccgo: response length is negative"); }
    if (responsePtr == 0 && responseLen != 0) { return rpccgoErrorResult(env, "rpccgo: response pointer is null"); }
    const uint8_t* data = reinterpret_cast<const uint8_t*>(responsePtr);
    std::vector<uint8_t> payload;
    if (responseLen != 0) { payload.assign(data, data + responseLen); }
    if (responsePtr != 0) { rpccgoRelease(responsePtr); }
    return rpccgoResult(env, true, payload);
}

jbyteArray rpccgoSuccessHandle(JNIEnv* env, int32_t handle) {
    std::vector<uint8_t> payload;
    rpccgoWriteInt32(&payload, handle);
    return rpccgoResult(env, true, payload);
}

jbyteArray rpccgoSuccessUnit(JNIEnv* env) {
    return rpccgoResult(env, true, {});
}

uintptr_t rpccgoVectorPtr(const std::vector<uint8_t>& data) {
    if (data.empty()) { return 0; }
    return reinterpret_cast<uintptr_t>(data.data());
}

std::string rpccgoErrorString(int32_t errID) {
    std::vector<uint8_t> text = rpccgoErrorText(errID);
    return std::string(text.begin(), text.end());
}

}  // namespace

// Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoServiceInfo invokes examples.android.foregroundservice.v1.ForegroundServiceDemo.ServiceInfo through the Android C++ JNI adapter.
extern "C" JNIEXPORT jbyteArray JNICALL Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoServiceInfo(JNIEnv* env, jobject, jbyteArray request) {
    rpccgoJNIEnvScope envScope(env);
    env = envScope.env;
    if (env == nullptr) { return nullptr; }
    bool requestOK = false;
    std::vector<uint8_t> requestBytes = rpccgoJNIBytes(env, request, &requestOK);
    if (!requestOK) { return rpccgoErrorResult(env, "rpccgo: JNI request bytes are null or unreadable"); }
    uintptr_t responsePtr = 0;
    int32_t responseLen = 0;
    int32_t errID = rpccgoMsgForegroundservicev1ForegroundServiceDemoServiceInfo(rpccgoVectorPtr(requestBytes), static_cast<int32_t>(requestBytes.size()), &responsePtr, &responseLen);
    if (errID != 0) { return rpccgoErrorIDResult(env, errID); }
    return rpccgoSuccessBytes(env, responsePtr, responseLen);
}

std::mutex foregroundServiceDemoWatchTicksCallbackMu;
jobject foregroundServiceDemoWatchTicksCallbackListener = nullptr;
jmethodID foregroundServiceDemoWatchTicksCallbackOnMessage = nullptr;
jmethodID foregroundServiceDemoWatchTicksCallbackOnDone = nullptr;
int32_t foregroundServiceDemoWatchTicksCallbackHandle = 0;

void clearForegroundServiceDemoWatchTicksListenerCallback(JNIEnv* env) {
    std::lock_guard<std::mutex> lock(foregroundServiceDemoWatchTicksCallbackMu);
    if (env != nullptr && foregroundServiceDemoWatchTicksCallbackListener != nullptr) {
        env->DeleteGlobalRef(foregroundServiceDemoWatchTicksCallbackListener);
    }
    foregroundServiceDemoWatchTicksCallbackListener = nullptr;
    foregroundServiceDemoWatchTicksCallbackOnMessage = nullptr;
    foregroundServiceDemoWatchTicksCallbackOnDone = nullptr;
    foregroundServiceDemoWatchTicksCallbackHandle = 0;
}

bool cancelForegroundServiceDemoWatchTicksListenerCallback(JNIEnv* env) {
    int32_t handle = 0;
    {
        std::lock_guard<std::mutex> lock(foregroundServiceDemoWatchTicksCallbackMu);
        handle = foregroundServiceDemoWatchTicksCallbackHandle;
    }
    if (handle == 0) {
        clearForegroundServiceDemoWatchTicksListenerCallback(env);
        return true;
    }
    int32_t errID = rpccgoMsgForegroundservicev1ForegroundServiceDemoWatchTicksCancel(handle);
    clearForegroundServiceDemoWatchTicksListenerCallback(env);
    return errID == 0;
}

int32_t onForegroundServiceDemoWatchTicksListenerMessage(int32_t, uintptr_t responsePtr, int32_t responseLen) {
    rpccgoJNIEnvScope envScope(nullptr);
    JNIEnv* env = envScope.env;
    if (env == nullptr) {
        if (responsePtr != 0) { rpccgoRelease(responsePtr); }
        return 0;
    }
    std::lock_guard<std::mutex> lock(foregroundServiceDemoWatchTicksCallbackMu);
    if (foregroundServiceDemoWatchTicksCallbackListener == nullptr || foregroundServiceDemoWatchTicksCallbackOnMessage == nullptr) {
        if (responsePtr != 0) { rpccgoRelease(responsePtr); }
        return 0;
    }
    jbyteArray payload = env->NewByteArray(responseLen);
    if (payload == nullptr) {
        if (responsePtr != 0) { rpccgoRelease(responsePtr); }
        return 0;
    }
    if (responseLen > 0) {
        env->SetByteArrayRegion(payload, 0, responseLen, reinterpret_cast<const jbyte*>(responsePtr));
    }
    if (responsePtr != 0) { rpccgoRelease(responsePtr); }
    env->CallVoidMethod(foregroundServiceDemoWatchTicksCallbackListener, foregroundServiceDemoWatchTicksCallbackOnMessage, payload);
    env->DeleteLocalRef(payload);
    if (env->ExceptionCheck()) { env->ExceptionClear(); }
    return 0;
}

int32_t onForegroundServiceDemoWatchTicksListenerDone(int32_t, int32_t errID) {
    rpccgoJNIEnvScope envScope(nullptr);
    JNIEnv* env = envScope.env;
    if (env == nullptr) { return 0; }
    jobject listener = nullptr;
    jmethodID onDone = nullptr;
    {
        std::lock_guard<std::mutex> lock(foregroundServiceDemoWatchTicksCallbackMu);
        listener = foregroundServiceDemoWatchTicksCallbackListener;
        onDone = foregroundServiceDemoWatchTicksCallbackOnDone;
    }
    if (listener != nullptr && onDone != nullptr) {
        jstring error = nullptr;
        if (errID != 0) {
            std::string text = rpccgoErrorString(errID);
            error = env->NewStringUTF(text.c_str());
        }
        env->CallVoidMethod(listener, onDone, error);
        if (error != nullptr) { env->DeleteLocalRef(error); }
        if (env->ExceptionCheck()) { env->ExceptionClear(); }
    }
    clearForegroundServiceDemoWatchTicksListenerCallback(env);
    return 0;
}

// Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksStart invokes examples.android.foregroundservice.v1.ForegroundServiceDemo.WatchTicks through the Android C++ JNI adapter.
extern "C" JNIEXPORT jbyteArray JNICALL Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksStart(JNIEnv* env, jobject, jbyteArray request) {
    rpccgoJNIEnvScope envScope(env);
    env = envScope.env;
    if (env == nullptr) { return nullptr; }
    bool requestOK = false;
    std::vector<uint8_t> requestBytes = rpccgoJNIBytes(env, request, &requestOK);
    if (!requestOK) { return rpccgoErrorResult(env, "rpccgo: JNI request bytes are null or unreadable"); }
    int32_t handle = 0;
    int32_t errID = rpccgoMsgForegroundservicev1ForegroundServiceDemoWatchTicksStart(rpccgoVectorPtr(requestBytes), static_cast<int32_t>(requestBytes.size()), &handle, nullptr, nullptr);
    if (errID != 0) { return rpccgoErrorIDResult(env, errID); }
    return rpccgoSuccessHandle(env, handle);
}

// Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksRecv invokes examples.android.foregroundservice.v1.ForegroundServiceDemo.WatchTicks through the Android C++ JNI adapter.
extern "C" JNIEXPORT jbyteArray JNICALL Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksRecv(JNIEnv* env, jobject, jint handle) {
    rpccgoJNIEnvScope envScope(env);
    env = envScope.env;
    if (env == nullptr) { return nullptr; }
    uintptr_t responsePtr = 0;
    int32_t responseLen = 0;
    int32_t errID = rpccgoMsgForegroundservicev1ForegroundServiceDemoWatchTicksRecv(static_cast<int32_t>(handle), &responsePtr, &responseLen);
    if (errID != 0) { return rpccgoErrorIDResult(env, errID); }
    return rpccgoSuccessBytes(env, responsePtr, responseLen);
}

// Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksCancel invokes examples.android.foregroundservice.v1.ForegroundServiceDemo.WatchTicks through the Android C++ JNI adapter.
extern "C" JNIEXPORT jbyteArray JNICALL Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksCancel(JNIEnv* env, jobject, jint handle) {
    rpccgoJNIEnvScope envScope(env);
    env = envScope.env;
    if (env == nullptr) { return nullptr; }
    int32_t errID = rpccgoMsgForegroundservicev1ForegroundServiceDemoWatchTicksCancel(static_cast<int32_t>(handle));
    if (errID != 0) { return rpccgoErrorIDResult(env, errID); }
    return rpccgoSuccessUnit(env);
}

// Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksStartCallback invokes examples.android.foregroundservice.v1.ForegroundServiceDemo.WatchTicks through the Android C++ JNI adapter.
extern "C" JNIEXPORT jboolean JNICALL Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksStartCallback(JNIEnv* env, jobject, jbyteArray request, jobject listener) {
    rpccgoJNIEnvScope envScope(env);
    env = envScope.env;
    if (env == nullptr) { return JNI_FALSE; }
    if (request == nullptr || listener == nullptr) { return JNI_FALSE; }
    cancelForegroundServiceDemoWatchTicksListenerCallback(env);
    jclass listenerClass = env->GetObjectClass(listener);
    jmethodID onMessage = env->GetMethodID(listenerClass, "onMessage", "([B)V");
    jmethodID onDone = env->GetMethodID(listenerClass, "onDone", "(Ljava/lang/String;)V");
    env->DeleteLocalRef(listenerClass);
    if (onMessage == nullptr || onDone == nullptr) { return JNI_FALSE; }
    bool requestOK = false;
    std::vector<uint8_t> requestBytes = rpccgoJNIBytes(env, request, &requestOK);
    if (!requestOK) { return JNI_FALSE; }
    jobject globalListener = env->NewGlobalRef(listener);
    if (globalListener == nullptr) { return JNI_FALSE; }
    {
        std::lock_guard<std::mutex> lock(foregroundServiceDemoWatchTicksCallbackMu);
        foregroundServiceDemoWatchTicksCallbackListener = globalListener;
        foregroundServiceDemoWatchTicksCallbackOnMessage = onMessage;
        foregroundServiceDemoWatchTicksCallbackOnDone = onDone;
    }
    int32_t handle = 0;
    int32_t errID = rpccgoMsgForegroundservicev1ForegroundServiceDemoWatchTicksStart(rpccgoVectorPtr(requestBytes), static_cast<int32_t>(requestBytes.size()), &handle, onForegroundServiceDemoWatchTicksListenerMessage, onForegroundServiceDemoWatchTicksListenerDone);
    if (errID != 0) {
        clearForegroundServiceDemoWatchTicksListenerCallback(env);
        return JNI_FALSE;
    }
    {
        std::lock_guard<std::mutex> lock(foregroundServiceDemoWatchTicksCallbackMu);
        foregroundServiceDemoWatchTicksCallbackHandle = handle;
    }
    return JNI_TRUE;
}

// Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksCancelCallback invokes examples.android.foregroundservice.v1.ForegroundServiceDemo.WatchTicks through the Android C++ JNI adapter.
extern "C" JNIEXPORT jboolean JNICALL Java_com_ygrpc_examples_rpccgoandroidforegroundservice_ForegroundServiceDemoJni_foregroundServiceDemoWatchTicksCancelCallback(JNIEnv* env, jobject) {
    rpccgoJNIEnvScope envScope(env);
    env = envScope.env;
    if (env == nullptr) { return JNI_FALSE; }
    return cancelForegroundServiceDemoWatchTicksListenerCallback(env) ? JNI_TRUE : JNI_FALSE;
}
